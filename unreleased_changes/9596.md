### Added
- Dataset voxel size and extent can now be clicked to copy the value to the clipboard in csv-friendly format.

### Changed
- Dates in admin tables are now formatted in a human-friendly way, e.g. hiding the year if it is the current one.
