### Fixed
- Fixed a bug where revoking the user auth token would lead to running worker jobs failing. They now use a separate token.

### Postgres Evolutions
- [177-token-type-job.sql](schema/evolutions/177-token-type-job.sql)
