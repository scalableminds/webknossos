### Fixed
- Fixed a race condition where reading data (e.g. via the JS API's `getDataValue`) right after reloading buckets could intermittently return empty data, because a concurrent reload could abort the bucket's pending request without the read retrying it.
