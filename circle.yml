machine:
  environment:
    SBT_VERSION_TAG: sbt-0.13.9_mongo-3.2.1_node-7.x_jdk-8

dependencies:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
    - curl -L https://github.com/docker/compose/releases/download/1.7.1/docker-compose-`uname -s`-`uname -m` > docker-compose
    - sudo mv docker-compose /usr/local/bin/docker-compose
    - sudo chmod +x /usr/local/bin/docker-compose
    - sudo service docker start
    - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
  cache_directories:
    - "~/.m2"
    - "~/.ivy2"
    - "~/.sbt"
    - "~/.yarn-cache"
    - "node_modules"
  override:
    - docker pull scalableminds/sbt:$SBT_VERSION_TAG
    - docker-compose run sbt clean compile stage
    - docker build -t scalableminds/webknossos:${CIRCLE_BRANCH}__${CIRCLE_BUILD_NUM} .

test:
  override:
    - docker-compose run webknossos-frontend-linting
    - docker-compose run webknossos-frontend-tests
    - docker-compose run webknossos-e2e-tests
    - DOCKER_TAG=${CIRCLE_BRANCH}__${CIRCLE_BUILD_NUM} docker-compose up -d webknossos:
        background: true
    - sleep 10
    - ./test/infrastructure/deployment.bash

deployment:
  hub:
    branch: /.*/
    commands:
      - docker logout
