### Changed
- Headings now use antd's `Typography.Title` instead of raw HTML heading tags, so that they are themed consistently through the antd design tokens. Heading sizes are unchanged, but headings now render at antd's default heading weight (600 instead of 500).
