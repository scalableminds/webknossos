### Added
- Show a warning when many chunks of data in an annotation are updated in a short period of time. The warning suggests creating an annotation (or an annotation layer) that excludes the finest magnification(s) to reduce server load.
