### Changed
- In AI model training, total credits are boldly highlighted and image data and ground truth layers are selected per default

### Fixed
- Fixed that theme couldn't be switched to "auto" (System-default) in account settings.
- Fixed word break for long layer names in Layer Setting Tab.
