### Changed
- Polished the AI jobs drawer: jobs are now split into numbered steps, models and tasks are picked from an image grid, training data errors are shown directly on each annotation, and a "Before you start" list explains why a job cannot be started yet.
