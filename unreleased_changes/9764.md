### Changed
- The 3D viewport now renders with a perspective camera by default. The projection can be switched between perspective and orthographic in the settings dropdown of the 3D viewport. Note that the scalebar of the 3D viewport is only exact at the rotation-center depth when the perspective projection is active.
