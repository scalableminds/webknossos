### Added
- Show a warning toast when many chunks of data in an annotation are updated in a short period of time. The toast suggests creating an annotation (or an annotation layer) that excludes the finest magnifications to reduce server load.
