### Changed
- Improved backend performance: added database indices for team-membership access checks, job-worker polling, folder-path lookups and credit transactions; closed leaked S3 clients on cache eviction; made per-user time-tracking state thread-safe; and reduced per-request allocations in NML parsing and binary-data clipping.

### Postgres Evolutions
- [175-team-and-job-indices.sql](schema/evolutions/175-team-and-job-indices.sql)
