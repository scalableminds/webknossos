### Added
- Added support for (un)signed int64 segmentations. Note that even signed segmentations only fully support positive segment ids. While negative ids can be rendered, interacting with them (including annotating them) is not supported.
