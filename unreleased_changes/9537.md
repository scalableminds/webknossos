### Added
- When merging annotations, users can now select to keep the segment IDs unchanged instead of remapping them.
- Reworked the merge annotations modal, adding explanations and clearer UI flow.
