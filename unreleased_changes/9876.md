### Added
- The segment statistics table now shows max distance, sphericity, center of mass and principal extents for segmentation layers that have a precomputed segment statistics file. The new columns are only offered for the metrics the file actually contains and that are compatible with the currently active mapping. The table is also available for such layers when they have no segment index.
