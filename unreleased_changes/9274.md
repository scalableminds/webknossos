### Changed
- Managed S3 paths are no longer deleted by WEBKNOSSOS directly, but by an external deletion service. Decreases wait times when users click delete dataset in the UI.

### Postgres Evolutions
- [154-paths-to-delete.sql](schema/evolutions/154-paths-to-delete.sql)
