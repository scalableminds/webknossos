### Fixed
- Fixed that label images of OME-Zarr 0.5 datasets were not found during exploration.
- Fixed wrong bounds and indices of additional axes (e.g. time) during OME-Zarr exploration when they did not come first in the axis order, e.g. for `c,t,z,y,x`.
