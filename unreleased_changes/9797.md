### Fixed
- Fixed that WEBKNOSSOS short links (e.g. `/links/:key`) could not be used to specify groundtruth annotations when training an AI model. Also fixed that the input validator for this field rejected plain annotation/task IDs.
