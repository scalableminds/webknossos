### Fixed
- Fixed that WEBKNOSSOS could render without any styling after a new version was deployed, which previously required a hard reload (Ctrl/Cmd + Shift + R) to recover from.

### Changed
- Content-hashed frontend assets are now cached indefinitely by the browser instead of for one hour.
