### Fixed
- Fixed a bug where pinging a datastore's health endpoint could throw an error, because a version check relied on a `datastoreApiVersion` field that the backend has stopped sending. Removed the stale field and check.
