### Fixed
- Fixed a bug where the mapped segment id at a position could be stale (e.g. after splits/merges during proofreading, when additional coordinates changed or when the visible segmentation layer changed), because the lookup was memoized only on the position.
