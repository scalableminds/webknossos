### Changed
- Failed or cancelled jobs can now be retried once by all users (previously only by super users). If a job fails again after a retry, users are hinted to contact an administrator for help.
