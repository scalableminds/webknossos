### Fixed
- Scrolling through slices with the mouse wheel is now clamped to the dataset's bounding box, so it's harder to accidentally scroll away from small datasets. Panning and keyboard-based movement are still unrestricted.
