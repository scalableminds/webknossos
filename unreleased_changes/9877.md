### Added
- Added a keyboard shortcut to navigate the first additional axis / 4th dimension (e.g. t axis). The shortcut has no default binding and needs to be manually set by the user via the keyboard shortcuts modal.