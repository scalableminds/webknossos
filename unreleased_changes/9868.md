### Added
- Sharing links now also restore the camera of the 3D viewport (position, rotation and zoom) as it was when the link was created. The projection type (perspective vs. orthographic) is not part of this, since it is a per-user setting.
