### Changed
- Refined the Info tab in the right sidebar: facts are now labelled key/value rows grouped under section headings, the annotation and dataset names act as the panel title with their edit buttons placed directly next to them, owner and contributors are plain text instead of tags, and the keyboard shortcuts are listed action-first with smaller, neutral keycaps.
- The bounding box count in the Info tab is now always shown for annotations, even when no bounding box exists yet.
