### Changed
- Rewrote the skeleton/trees tab as a modular, hook-based React component. Functionality is unchanged.

### Fixed
- Fixed a rare bug where ctrl-clicking a tree to start a multi-selection could immediately clear the selection again.
