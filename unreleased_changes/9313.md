### Added
- Added new scripts in ./tools/segmentation-id-modification that allow incrementing IDs in segmentation layers and agglomerate files (useful for testing compatibility with certain dtypes).

### Fixed
- Fixed bug when proofreading with IDs > 2^32 in uint64 segmentations.
