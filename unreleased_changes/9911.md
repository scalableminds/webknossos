### Added
- Added an "Apply View Configuration to All Datasets in Folder" entry to the dataset context menu in the dashboard. It copies the view configuration of the selected dataset to all other datasets in the same folder. Layer settings are only copied to layers of the same name and category.
