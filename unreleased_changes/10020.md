### Fixed
- Fixed that mappings for uint64 segmentation layers could fail with "Cannot rehash, since 20 attempts were exceeded", because 64-bit segment ids were hashed with a poorly distributed hash function.
