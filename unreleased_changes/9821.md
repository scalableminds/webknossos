### Changed
- The initial zoom value when viewing a dataset is now adjusted so that the finest magnification which all layers of the dataset share is rendered. This only happens if no zoom value was configured via the URL, the annotation or the view configuration.
