### Changed
- The view configuration of an annotation is now stored per user (instead of per annotation). Thus, if the owner disables a layer in a shared editable annotation, this does not transfer over to other users who have already seen the annotation.

### Postgres Evolutions
- [169-per-user-annotation-view-config.sql](schema/evolutions/169-per-user-annotation-view-config.sql)
